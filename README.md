# HackerRank-Contest-Leaderboard-to-EXCEL
This repo contains a simple python script to create HackerRank contest leaderboard in excel format. 
* Installation
run `pip install csv requests browser-cookie3` to install dependencies

- After Installing the Dependencies
- run `python hacker_scrap.py` in windows and `python3 hacker_scrap.py` in linux.
